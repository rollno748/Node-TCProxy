var net = require('net');
var tls = require('tls');
var fs = require('fs');
var util = require('util');

module.exports.createProxy = function(proxyPort,
    targetHost, targetPort, options) {
    //return new TcpProxy(proxyPort, targetHost, targetPort, options);
    return null
};
